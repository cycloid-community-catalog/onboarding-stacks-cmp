#
# Azure credentials are provided by the managed environment:
# - Cloud account (azure): env_providers.azure.client_id, client_secret, tenant_id, subscription_id
#


#
# Azure Function
#

#. git_func_url (required): ""
#+ The git repository URL where the azure function to deploy is located
git_func_url: ""

#. git_func_branch (required): ""
#+ The git repository branch where the azure function to deploy is located
git_func_branch: ""


#
# Default variables
#

#. terraform_version (required): '1.8.3'
#+ terraform version used to execute your code.
terraform_version: '1.8.3'
