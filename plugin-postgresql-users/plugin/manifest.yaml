description: "Read-only iframe table listing PostgreSQL database users."
icon: "https://cycloid-demo-stacks-logo.s3.eu-west-1.amazonaws.com/postgresql.svg"

configuration:
  - name: "Database URL"
    description: "PostgreSQL connection URL (postgresql://user:password@host:5432/database)."
    key: database_url
    widget: simple_text
    type: string
    required: true
